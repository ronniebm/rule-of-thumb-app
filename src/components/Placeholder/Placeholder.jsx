const Placeholder = ({ title }) => {
    return (
        <section className="placeholder">
            <h1>
                { title }
            </h1>
        </section>
    )
}

export default Placeholder;
